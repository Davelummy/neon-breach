{
  "mcpServers": {
    "elevenlabs": {
      "command": "uvx",
      "args": ["elevenlabs-mcp"],
      "env": {
        "ELEVENLABS_API_KEY": "${ELEVENLABS_API_KEY}",
        "ELEVENLABS_MCP_BASE_PATH": "${ELEVENLABS_MCP_BASE_PATH:-/Users/davidolumide/neon-breach/.codex/artifacts/audio}",
        "ELEVENLABS_MCP_OUTPUT_MODE": "${ELEVENLABS_MCP_OUTPUT_MODE:-files}"
      }
    }
  }
}
